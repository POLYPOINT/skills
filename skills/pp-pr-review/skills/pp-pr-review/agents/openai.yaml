interface:
  display_name: "POLYPOINT PR Review"
  short_description: "Run a gated Azure DevOps PR review"
  default_prompt: "Use $pp-pr-review to review this Azure DevOps pull request."

policy:
  allow_implicit_invocation: true
