{
  "name": "pep-verify",
  "description": "Drive and verify things directly in PEP (POLYPOINT's Delphi/VCL personnel-planning Windows client) on a tenant box over RDP/SSH — element-level automation (pywinauto over SSH+loopback), not pixel clicking. Bootstraps the companion pep-driver toolchain, then runs selector-based UI commands and flows.",
  "version": "1.0.1"
}
